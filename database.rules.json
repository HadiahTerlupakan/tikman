{
  "rules": {
    "cs-presence": {
      ".read": "auth != null && auth.token.tikman === true",
      "$uid": {
        ".write": "auth != null && auth.token.tikman === true && auth.uid === $uid",
        ".validate": "newData.hasChildren(['at'])"
      }
    }
  }
}
